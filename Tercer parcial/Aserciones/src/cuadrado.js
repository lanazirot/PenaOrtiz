/**
 * 
 * Elevar un numero base a un exponente
 * 
 * @author Alan Pena
 * @param {Number} numberoBase 
 * @param {Number} exponente 
 * @returns El numero de la base elevado al exponente
 */
const potencia = (numberoBase, exponente) => numberoBase ** exponente; 

module.exports = potencia;